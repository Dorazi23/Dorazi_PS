#include <bits/stdc++.h>
#define endl '\n'
#define X first
#define Y second
#define fastio cin.tie(0)->sync_with_stdio(0)
#define MAX 1000000000
#define INF 4e18
using namespace std;
using ll = long long;

void input () {
    double a; cin >> a;
    cout << fixed << setprecision(10) << a / 6 << endl;
}

int main() {
    fastio;
    int t; cin >> t;
    while (t--) {
        input();   
    }
}